spring.application.name=pos
spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/posdb
    username: posuser
    password: strongpassword

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

server:
  port: 8080